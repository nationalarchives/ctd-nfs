[project]
name = "ctd-nfs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cx-freeze>=8.6.2",
    "openpyxl>=3.1.5",
    "pandas>=3.0.2",
    "pyyaml>=6.0.3",
    "requests>=2.32.5",
    "xlutils>=2.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.pytest]
minversion = "9.0"
# addopts = ["-ra", "-q"]
testpaths = [
    "tests",
]
